'use strict';

import React from 'react/addons';
import SearchBar from '../UI/SearchBar';

let InterfaceSearch = React.createClass({
    render() {

        return (
            <div>
                <SearchBar />
            </div>
        );
    }
});

module.exports = InterfaceSearch;
